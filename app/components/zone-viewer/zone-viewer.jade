div.zoneViewer
  h3 {{zone.dnsName}}
  small {{ zone.description }}
  dl
    dt Name:
    dd {{ zone.name }}
    dt Created:
    dd {{ zone.creationTime | moment:'DD MMM, YYYY' }}
    dt Name Servers:
    dd(ng-repeat="ns in zone.nameServers") {{ ns }}