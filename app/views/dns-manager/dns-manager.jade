section(layout="horizontal" layout-padding).dnsManager
  md-sidenav(is-locked-open="$media('md')" component-id="zone-list-nav").md-sidenav-left.md-whiteframe-z2
    md-toolbar(md-theme="grey" layout="horizontal")
      .md-toolbar-tools
        h3(flex)
          span Zones
        md-button.md-fab(aria-label="Add Zone" ng-click="dm.createZone()") +

    md-content.md-padding
      zone-list(zones="dm.zoneModel.zoneList" selectedZone="dm.zoneModel.selectedZone")
  ui-view
